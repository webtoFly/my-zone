type Methods = 'GET' | 'POST'
const BASE_URL = '';

class Http {
    constructor() {
    }

    request({url, method, params, data, options}) {

    }
}
const httpRequest = new Http();
console.log(httpRequest, 'httpRequest');
export default httpRequest;
